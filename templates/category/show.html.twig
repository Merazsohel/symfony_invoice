{% extends 'base.html.twig' %}

{% block title %}Category{% endblock %}

{% block body %}

<div class="row">
    <div class="container">
        <div class="col-md-12 mt-5">
    <table class="table table-bordered">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ category.name }}</td>
            </tr>
        </tbody>
    </table>
        </div>
        <a href="{{ path('category_index') }}" class="btn btn-danger ml-3">Back</a>
        <div class="mt-3 ml-3">
            {{ include('category/_delete_form.html.twig') }}
        </div>
    </div>

</div>



{% endblock %}
