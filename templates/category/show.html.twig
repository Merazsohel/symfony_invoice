{% extends 'base.html.twig' %}

{% block title %}Category{% endblock %}

{% block body %}

<div class="row">
    <div class="container">
        <div class="col-md-12 mt-5">
    <table class="table table-bordered">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ category.name }}</td>
            </tr>
        </tbody>
    </table>
        </div>
        <a href="{{ path('category_index') }}" class="btn btn-info ml-3">back to list</a>
    </div>

</div>



{% endblock %}
