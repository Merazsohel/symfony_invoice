{% extends 'base.html.twig' %}

{% block title %}New Product{% endblock %}

{% block body %}
<div class="container py-5">
    <div class="row">
        <div class="col-md-12">
            <div class="row">
                <div class="col-md-6 mx-auto">
                    <div class="card border-secondary">
                        <div class="card-header">
                            <h3 class="mb-0 my-2"> New Product</h3>
                        </div>
                        <div class="card-body">

                            {{ include('product/_form.html.twig') }}

                            <a href="{{ path('product_index') }}" class="btn btn-info mt-3">Back to list</a>

                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
